# ============================================================
# ArXiv Agent 配置文件 - DeepSeek 示例
# ============================================================

# ============================================================
# 1. 研究方向配置
# ============================================================
research_interests:
  - 自动驾驶
  - 具身智能
  - 强化学习
  - VLM (Vision Language Models)
  - VLA (Vision Language Action Models)
  - 大语言模型
  - 世界模型

# 研究兴趣详细描述（可选，优先用于相关性分析）
research_prompt: |
  我最近关注自动驾驶和具身智能领域，特别是以下几个方向：

  1. 端到端自动驾驶系统，尤其是使用视觉语言模型(VLM)和世界模型进行决策的方法
  2. 具身智能中的Vision-Language-Action (VLA)模型，能够将视觉输入和语言指令转化为机器人动作
  3. 强化学习在自动驾驶场景中的应用，包括离线强化学习、模仿学习等
  4. 大语言模型在机器人任务规划和推理中的应用
  5. 除了上述以外，我还关注一些尚未在自动驾驶和具身智能中广泛应用但已经在VLM、强化学习、大语言模型的其他应用场景中取得突破性进展的方法，且有望迁移到自动驾驶和具身智能领域。

  我对理论创新和实际应用都感兴趣，尤其关注能够在真实世界部署的方法。

# ============================================================
# 2. 数据源配置
# ============================================================
sources:
  # ArXiv论文源
  arxiv:
    enabled: true
    categories:  # 搜索的ArXiv类别
      - cs.RO  # Robotics
      - cs.CV  # Computer Vision
      - cs.AI  # Artificial Intelligence
      - cs.LG  # Machine Learning
      - cs.CL  # Computation and Language
    max_results: 50  # 每个类别最多获取多少篇
    days_back: 3     # 搜索最近几天

  # 学术期刊源（Nature、Science、Cell系列）
  journals:
    enabled: true
    days_back: 7
    selected_journals:  # 只获取这些期刊（留空则获取所有可用期刊）
      - Nature
      - Science
      - Nature Machine Intelligence
      - Science Robotics
      - Nature Communications

# ============================================================
# 3. LLM API 配置 - DeepSeek（便宜且效果好）
# ============================================================
# API类型：anthropic 或 openai
api_type: openai

# DeepSeek API 配置
api_base_url: https://api.deepseek.com
api_key: YOUR_DEEPSEEK_API_KEY  # 在 https://platform.deepseek.com/ 获取

# 模型配置
# DeepSeek 推荐模型：
#   - deepseek-chat: 最新通用模型 (¥0.27/M输入, ¥1.1/M输出)
#   - deepseek-reasoner: 深度思考模型 (¥0.55/M输入, ¥2.19/M输出)
model: deepseek-chat
max_tokens: 2048  # DeepSeek 支持更大的 token 数

# ============================================================
# 4. 筛选与性能配置
# ============================================================
# 相关性阈值：high（只要高相关）、medium（中相关及以上）、low（低相关及以上）
min_relevance: medium

# 并发性能配置
max_concurrent: 10     # DeepSeek 便宜，可以提高并发数（建议10-20）
batch_size: 30         # 第一阶段批量筛选时每批论文数量
detail_batch_size: 10  # 第二阶段详细分析时每批论文数量

# ============================================================
# 5. 输出与通知配置
# ============================================================
output_dir: reports

# 邮件通知配置（可选）
email:
  enabled: false
  smtp_server: smtp.163.com
  smtp_port: 465
  use_ssl: true
  sender_email: your_email@163.com
  sender_password: YOUR_PASSWORD
  receiver_email: receiver@gmail.com
  subject_prefix: "[ArXiv每日论文]"
