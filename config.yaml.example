# ============================================================
# ArXiv Agent 配置文件示例
# ============================================================
# 使用说明：
# 1. 复制此文件为 config.yaml
# 2. 填入你的 API 密钥和邮箱配置
# 3. 根据需要修改研究方向和数据源配置

# ============================================================
# 1. 研究方向配置
# ============================================================
research_interests:
  - 自动驾驶
  - 具身智能
  - 强化学习
  - VLM (Vision Language Models)
  - VLA (Vision Language Action Models)
  - 大语言模型
  - 世界模型

# 研究兴趣详细描述（可选，优先用于相关性分析）
research_prompt: |
  我最近关注自动驾驶和具身智能领域，特别是以下几个方向：

  1. 端到端自动驾驶系统，尤其是使用视觉语言模型(VLM)和世界模型进行决策的方法
  2. 具身智能中的Vision-Language-Action (VLA)模型，能够将视觉输入和语言指令转化为机器人动作
  3. 强化学习在自动驾驶场景中的应用，包括离线强化学习、模仿学习等
  4. 大语言模型在机器人任务规划和推理中的应用
  5. 除了上述以外，我还关注一些尚未在自动驾驶和具身智能中广泛应用但已经在VLM、强化学习、大语言模型的其他应用场景中取得突破性进展的方法，且有望迁移到自动驾驶和具身智能领域。

  我对理论创新和实际应用都感兴趣，尤其关注能够在真实世界部署的方法。

# ============================================================
# 2. 数据源配置
# ============================================================
sources:
  # ArXiv论文源
  arxiv:
    enabled: true
    categories:  # 搜索的ArXiv类别
      - cs.RO  # Robotics
      - cs.CV  # Computer Vision
      - cs.AI  # Artificial Intelligence
      - cs.LG  # Machine Learning
      - cs.CL  # Computation and Language
    max_results: 50  # 每个类别最多获取多少篇
    days_back: 3     # 搜索最近几天

  # 学术期刊源（Nature、Science、Cell系列）
  journals:
    enabled: true
    days_back: 7
    selected_journals:  # 只获取这些期刊（留空则获取所有可用期刊）
      - Nature
      - Science
      - Nature Machine Intelligence
      - Science Robotics
      - Nature Communications

  # Twitter推文源（Selenium浏览器爬虫，无需API）
  twitter:
    enabled: false  # 改为 true 启用（需要安装Chrome浏览器）
    days_back: 7
    tweets_per_user: 2
    following_usernames:
      # 示例：10个常用学术账号
      - karpathy        # Andrej Karpathy (Eureka Labs)
      - OpenAI          # OpenAI官方
      - ylecun          # Yann LeCun (Meta AI)
      - DeepMind        # DeepMind官方
      - chelseabfinn    # Chelsea Finn (Stanford)
      - danijarh        # Danijar Hafner (世界模型)
      - _akhaliq        # AK (每日AI论文)
      - hardmaru        # David Ha (世界模型)
      - svlevine        # Sergey Levine (UC Berkeley)
      - NVIDIAAI        # NVIDIA AI官方
      # 更多50个账号配置见 config.yaml 或 README.md

# ============================================================
# 3. OpenAI API 配置
# ============================================================
# API端点
# - 官方API: https://api.openai.com/v1
# - 第三方代理（如ChatAnywhere）: https://api.chatanywhere.tech/v1 或 https://api.chatanywhere.org/v1
base_url: https://api.openai.com/v1

# API密钥（也可以通过环境变量 OPENAI_API_KEY 设置）
# 获取地址：https://platform.openai.com/account/api-keys
api_key: your-openai-api-key-here  # 替换为你的 OpenAI API 密钥

# 模型配置
# 推荐模型：
#   - gpt-4o: 最强大的多模态模型
#   - gpt-4-turbo: 性能与成本均衡
#   - gpt-4o-mini: 快速且经济的模型
#   - gpt-3.5-turbo: 经济实惠的选择
model: gpt-4o
max_tokens: 4096

# ============================================================
# 4. 筛选与性能配置
# ============================================================
# 相关性阈值：high（只要高相关）、medium（中相关及以上）、low（低相关及以上）
min_relevance: medium

# 并发性能配置
max_concurrent: 5      # 最大并发请求数（建议5-10）
batch_size: 15         # 第一阶段批量筛选时每批论文数量（建议15-20，较小值可提高筛选准确性）
detail_batch_size: 5   # 第二阶段详细分析时每批论文数量（建议5-10）

# ============================================================
# 5. 输出与通知配置
# ============================================================
output_dir: reports

# 邮件通知配置（可选）
email:
  enabled: true
  smtp_server: smtp.163.com       # SMTP服务器
  smtp_port: 465                  # SMTP端口
  use_ssl: true
  sender_email: your_email@163.com          # 发送邮箱，替换为你的邮箱
  sender_password: your_email_auth_code     # 邮箱授权码（不是登录密码），替换为你的授权码
  receiver_email: receiver@gmail.com        # 接收邮箱，替换为你的接收邮箱
  subject_prefix: "[ArXiv每日论文]"
